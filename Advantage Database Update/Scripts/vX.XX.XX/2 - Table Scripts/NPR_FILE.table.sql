IF NOT EXISTS(SELECT TABLE_NAME FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_NAME = 'NPR_FILE') BEGIN
    CREATE TABLE [dbo].[NPR_FILE](
	    [NPR_FILE_ID] [int] IDENTITY(1,1) NOT NULL,
	    [FILENAME] [varchar](200) NOT NULL,
	    [PROCESSED_TIME] [smalldatetime] NULL,
     CONSTRAINT [PK_NPR_FILE] PRIMARY KEY CLUSTERED 
    (
	    [NPR_FILE_ID] ASC
    )WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON, FILLFACTOR = 90) ON [PRIMARY]
    ) ON [PRIMARY]
END
GO
