IF EXISTS(SELECT TABLE_NAME FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_NAME = 'PROOFING_X_REVIEWER') 
BEGIN
   DROP TABLE [dbo].[PROOFING_X_REVIEWER];  
END
GO
IF NOT EXISTS(SELECT TABLE_NAME FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_NAME = 'PROOFING_X_REVIEWER') 
BEGIN
	CREATE TABLE [dbo].[PROOFING_X_REVIEWER](
		[ID] [int] IDENTITY(1,1) NOT NULL,
		[EMAIL] [varchar](1000) NOT NULL,
		[NAME] [varchar](300) NOT NULL,
		[CL_CODE] [VARCHAR] (6) NULL,
		[DIV_CODE] [VARCHAR] (6) NULL,
		[PRD_CODE] [VARCHAR] (6) NULL,
        [JOB_NUMBER] INT NULL,
        [JOB_COMPONENT_NBR] SMALLINT NULL,
        [ADDED_BY_USER_CODE] VARCHAR(100) NULL,
        [ADDED_DATE] SMALLDATETIME NULL,
        [IS_ACTIVE] BIT NULL
	 CONSTRAINT [PK_PROOFING_X_REVIEWER] PRIMARY KEY CLUSTERED 
	(
		[ID] ASC
	)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON) ON [PRIMARY]
	) ON [PRIMARY]
END
GO
