exec advsp_sql_column_prepare 'MEDIA_BROADCAST_WORKSHEET_PRINT_OPTIONS', 'PRINT_ZERO_SPOT_LINES'
GO

IF EXISTS (SELECT COLUMN_NAME FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME = 'MEDIA_BROADCAST_WORKSHEET_PRINT_OPTIONS' AND COLUMN_NAME = 'PRINT_ZERO_SPOT_LINES')  BEGIN

	   ALTER TABLE [dbo].[MEDIA_BROADCAST_WORKSHEET_PRINT_OPTIONS] DROP COLUMN [PRINT_ZERO_SPOT_LINES]
	
END
GO

IF NOT EXISTS(SELECT COLUMN_NAME FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME = 'MEDIA_BROADCAST_WORKSHEET_PRINT_OPTIONS' AND COLUMN_NAME = 'PRINT_REMOVE_LINES_WITHOUT_SPOTS') BEGIN

	ALTER TABLE [dbo].[MEDIA_BROADCAST_WORKSHEET_PRINT_OPTIONS] ADD [PRINT_REMOVE_LINES_WITHOUT_SPOTS] bit NOT NULL DEFAULT(0)
		
END
GO