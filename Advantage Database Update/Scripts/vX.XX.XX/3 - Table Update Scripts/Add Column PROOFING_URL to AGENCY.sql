IF NOT EXISTS(	SELECT 1
				FROM   
					INFORMATION_SCHEMA.COLUMNS
				WHERE  
					TABLE_NAME = 'AGENCY'
					AND COLUMN_NAME = 'PROOFING_URL') 
BEGIN
	ALTER TABLE AGENCY
	ADD PROOFING_URL VARCHAR(256) NULL;
END
GO
