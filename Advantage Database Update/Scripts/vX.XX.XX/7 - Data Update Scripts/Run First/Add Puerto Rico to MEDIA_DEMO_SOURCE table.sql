IF NOT EXISTS(SELECT 1 FROM dbo.MEDIA_DEMO_SOURCE WHERE [NAME] = 'Nielsen Puerto Rico') BEGIN

	INSERT INTO
		dbo.MEDIA_DEMO_SOURCE(MEDIA_DEMO_SOURCE_ID, [NAME])
	VALUES
		(4, 'Nielsen Puerto Rico')

END
