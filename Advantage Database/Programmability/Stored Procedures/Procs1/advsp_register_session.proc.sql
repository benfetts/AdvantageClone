CREATE PROCEDURE [dbo].[advsp_register_session]
@sec_user_id INT, @menu_hwnd_in INT, @web_id_in VARCHAR (255), @time_hwnd_in INT, @connect BIT, @ret_val INT OUTPUT
WITH ENCRYPTION
AS
BEGIN
--The script body was encrypted and cannot be reproduced here.
    RETURN
END


