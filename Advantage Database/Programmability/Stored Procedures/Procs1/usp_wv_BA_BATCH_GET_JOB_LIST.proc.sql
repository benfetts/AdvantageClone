SET QUOTED_IDENTIFIER ON 
GO
SET ANSI_NULLS OFF 
GO
IF EXISTS (SELECT * FROM dbo.sysobjects WHERE id = object_id(N'[dbo].[usp_wv_BA_BATCH_GET_JOB_LIST]') AND OBJECTPROPERTY(id, N'IsProcedure') = 1)
DROP PROCEDURE [dbo].[usp_wv_BA_BATCH_GET_JOB_LIST]
GO
CREATE PROCEDURE [dbo].[usp_wv_BA_BATCH_GET_JOB_LIST] /*WITH ENCRYPTION*/
@BA_BATCH_ID AS INT,
@INCLUDE_APPROVAL_JOBS AS BIT
AS
/*=========== QUERY ===========*/

	SELECT * 
	FROM [dbo].[wvtf_get_batch_job_list](@BA_BATCH_ID, @INCLUDE_APPROVAL_JOBS)
	ORDER BY JOB_NUMBER, JOB_COMPONENT_NBR;

/*=========== QUERY ===========*/
GO
SET QUOTED_IDENTIFIER ON 
GO
SET ANSI_NULLS ON 
GO