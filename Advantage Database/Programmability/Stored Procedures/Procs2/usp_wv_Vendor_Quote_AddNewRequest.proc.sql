

CREATE PROCEDURE [dbo].[usp_wv_Vendor_Quote_AddNewRequest]
(
	@ESTIMATE_NUMBER INT,
	@EST_COMPONENT_NBR SMALLINT,
	@VENDOR_QTE_NBR INT,
	@VENDOR_QTE_DESC VARCHAR(60),
	@VN_QTE_MEMO TEXT,
	@CREATED_BY VARCHAR(100),
	@CREATE_DATE smalldatetime,
	@USER_ID VARCHAR(100)
)
AS
BEGIN
	SET NOCOUNT OFF
	
	DECLARE 
	@ERR INT--,
	--@NEXT_EST_NBR INT
	
    --SELECT @NEXT_EST_NBR = ISNULL(MAX(ESTIMATE_NUMBER),-1) + 1 FROM ESTIMATE_LOG 
    
    INSERT INTO [VENDOR_QTE_HDR]
		(
			[ESTIMATE_NUMBER],
			[EST_COMPONENT_NBR],
			[VENDOR_QTE_NBR],
			[VENDOR_QTE_DESC],
			[VN_QTE_MEMO],
			[CREATED_BY],
			[CREATE_DATE]
		)
		VALUES
		(
			@ESTIMATE_NUMBER,
			@EST_COMPONENT_NBR,
			@VENDOR_QTE_NBR,
			@VENDOR_QTE_DESC,
			@VN_QTE_MEMO,
			@CREATED_BY,
			@CREATE_DATE
		)

	
	SET @ERR = @@Error

	--RETURN @ERR
END




