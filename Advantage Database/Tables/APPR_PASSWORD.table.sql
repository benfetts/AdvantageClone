CREATE TABLE [dbo].[APPR_PASSWORD] (
    [ACCT_EXEC]    VARCHAR (6)   NOT NULL,
    [PASSWORD_ID]  VARCHAR (20)  NOT NULL,
    [USER_ID]      VARCHAR (100) NOT NULL,
    [DATE_CREATED] SMALLDATETIME DEFAULT (getdate()) NULL,
    [INACTIVE]     SMALLINT      NULL
);

