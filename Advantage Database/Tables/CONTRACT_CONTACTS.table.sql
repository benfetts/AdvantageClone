--GO
--GO
--GO
CREATE TABLE [dbo].[CONTRACT_CONTACTS](
	[CONTRACT_CONTACT_ID] [int]  IDENTITY(1,1) NOT NULL,
	[CONTRACT_ID] [int] NOT NULL,
	[EMP_CODE] [varchar](6) NOT NULL,
	[INCLUDE_IN_ALERT] [bit] NOT NULL,
 CONSTRAINT [PK_CONTRACT_CONTACTS] PRIMARY KEY CLUSTERED 
(
	[CONTRACT_CONTACT_ID] ASC
)WITH (PAD_INDEX  = OFF, STATISTICS_NORECOMPUTE  = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS  = ON, ALLOW_PAGE_LOCKS  = ON) ON [PRIMARY]
) ON [PRIMARY]
GO

ALTER TABLE [dbo].[CONTRACT_CONTACTS] ADD UNIQUE([CONTRACT_ID], [EMP_CODE])
GO

ALTER TABLE [dbo].[CONTRACT_CONTACTS] ADD CONSTRAINT [FK_CONTRACT_CONTACTS_CONTRACT]
	FOREIGN KEY([CONTRACT_ID]) REFERENCES [dbo].[CONTRACT] ([CONTRACT_ID])
GO

ALTER TABLE [dbo].[CONTRACT_CONTACTS] ADD CONSTRAINT [FK_CONTRACT_CONTACTS_EMP_CODE]
	FOREIGN KEY([EMP_CODE]) REFERENCES [dbo].EMPLOYEE_CLOAK ([EMP_CODE])
GO
