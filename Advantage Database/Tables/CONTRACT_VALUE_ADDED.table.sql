--GO
--GO
CREATE TABLE [dbo].[CONTRACT_VALUE_ADDED](
	[VALUE_ADDED_ID] [int] IDENTITY(1,1) NOT NULL,
	[CONTRACT_ID] [int] NOT NULL,
	[DESCRIPTION] [varchar](100) NOT NULL,
	[COMMENT] [varchar](max) NULL,
	[DOCUMENT_ID] [int] NULL,
 CONSTRAINT [PK_CONTRACT_VALUE_ADDED] PRIMARY KEY CLUSTERED 
(
	[VALUE_ADDED_ID] ASC
)WITH (PAD_INDEX  = OFF, STATISTICS_NORECOMPUTE  = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS  = ON, ALLOW_PAGE_LOCKS  = ON) ON [PRIMARY]
) ON [PRIMARY]
GO

ALTER TABLE [dbo].[CONTRACT_VALUE_ADDED] ADD CONSTRAINT [FK_CONTRACT_VALUE_ADDED_CONTRACT]
	FOREIGN KEY([CONTRACT_ID]) REFERENCES [dbo].[CONTRACT] ([CONTRACT_ID])
GO
ALTER TABLE [dbo].[CONTRACT_VALUE_ADDED] ADD CONSTRAINT [FK_CONTRACT_VALUE_ADDED_DOCUMENT]
	FOREIGN KEY([DOCUMENT_ID]) REFERENCES [dbo].[DOCUMENTS] ([DOCUMENT_ID])
GO