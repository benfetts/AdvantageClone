CREATE TABLE [dbo].[CURRENCY_DETAIL] (
    [CURRENCY_ID]   INT             IDENTITY (1, 1) NOT NULL,
    [CURRENCY_CODE] VARCHAR (5)     NOT NULL,
    [EXCHANGE_RATE] DECIMAL (12, 6) NOT NULL,
    [EXCHANGE_DATE] SMALLDATETIME   NOT NULL
);

