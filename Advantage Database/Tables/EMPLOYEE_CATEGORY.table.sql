CREATE TABLE [dbo].[EMPLOYEE_CATEGORY] (
    [EMPLOYEE_CATEGORY_ID] INT          IDENTITY (1, 1) NOT NULL,
    [EMPLOYEE_CATEGORY]    VARCHAR (50) NOT NULL,
    [INACTIVE_FLAG]        SMALLINT     NULL
);

