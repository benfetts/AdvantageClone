CREATE TABLE [dbo].[JOB_SPEC_CATEGORY] (
    [CATEGORY_ID]    INT          NOT NULL,
    [CATEGORY_DESC]  VARCHAR (32) NOT NULL,
    [SPEC_TYPE_CODE] VARCHAR (6)  NOT NULL,
    [CATEGORY_SEQ]   SMALLINT     NOT NULL,
    [INACTIVE_FLAG]  SMALLINT     DEFAULT (0) NOT NULL
);

