CREATE TABLE [dbo].[MAR_ARTIST_DTL] (
    [MAR_NUMBER]   INT             NOT NULL,
    [REV_NBR]      SMALLINT        NOT NULL,
    [LINE_NUMBER]  INT             NOT NULL,
    [SC_CODE]      VARCHAR (6)     NOT NULL,
    [SC_SPLIT_AMT] DECIMAL (15, 2) NOT NULL
);

