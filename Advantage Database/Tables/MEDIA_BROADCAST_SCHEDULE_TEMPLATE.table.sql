IF NOT EXISTS(SELECT TABLE_NAME FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_NAME = 'MEDIA_BROADCAST_SCHEDULE_TEMPLATE') BEGIN

	CREATE TABLE [dbo].[MEDIA_BROADCAST_SCHEDULE_TEMPLATE](
		[MEDIA_BROADCAST_SCHEDULE_TEMPLATE_ID] [int] IDENTITY(1,1) NOT NULL,
		[MEDIA_BROADCAST_WORKSHEET_ID] [int] NOT NULL,
		[TEMPLATE_NAME] [varchar](50) NOT NULL,
		[RUN_DETAIL] [bit] NOT NULL,
		[RUN_MARKET] [bit] NOT NULL,
		[RUN_STATION] [bit] NOT NULL,
		[RUN_WEEKLY_DAILY] [bit] NOT NULL,
		[LOCATION] [varchar](50) NOT NULL,
		[SHOW_SECONDARY] [bit] NOT NULL,

		[SHOW_RATINGS] [bit] NOT NULL,
		[SHOW_COMMENTS] [bit] NOT NULL,
		[SHOW_SPOTCOSTS] [bit] NOT NULL,
		[SHOW_IMPRESSIONS] [bit] NOT NULL,
		[SHOW_BOOKSENDS] [bit] NOT NULL,
		[SHOW_TOTALCOSTS] [bit] NOT NULL,
		[SHOW_CPPM] [bit] NOT NULL,
		[SHOW_ADDEDVALUE] [bit] NOT NULL,
		[SHOW_RF] [bit] NOT NULL,

		[DATE_FROM] [varchar](50) NOT NULL,
		[DATE_TO] [varchar](50) NOT NULL,
		[MARKETS] [varchar](max) NOT NULL,
		[VENDORS] [varchar](max) NOT NULL,
	 CONSTRAINT [PK_MEDIA_BROADCAST_SCHEDULE_TEMPLATE] PRIMARY KEY CLUSTERED 
	(
		[MEDIA_BROADCAST_SCHEDULE_TEMPLATE_ID] ASC
	)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON) ON [PRIMARY]
	) ON [PRIMARY] TEXTIMAGE_ON [PRIMARY]

END