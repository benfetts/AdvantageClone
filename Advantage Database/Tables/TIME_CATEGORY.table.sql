CREATE TABLE [dbo].[TIME_CATEGORY] (
    [CATEGORY]      VARCHAR (10) NOT NULL,
    [DESCRIPTION]   VARCHAR (40) NULL,
    [VAC_SICK_FLAG] SMALLINT     NULL,
    [INACTIVE_FLAG] SMALLINT     NULL
);

