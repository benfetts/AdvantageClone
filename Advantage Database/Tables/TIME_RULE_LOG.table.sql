CREATE TABLE [dbo].[TIME_RULE_LOG] (
    [TIME_RULE_LOG_ID]   INT           IDENTITY (1, 1) NOT NULL,
    [LOG_DATETIME_STAMP] DATETIME      DEFAULT (getdate()) NOT NULL,
    [PROCESSED_BY]       VARCHAR (100) NOT NULL,
    [PROCESS_MONTH]      TINYINT       NULL,
    [PROCESS_YEAR]       SMALLINT      NULL
);

