CREATE TABLE [dbo].[W_EMP_STD_HOURS] (
    [USERID]     VARCHAR (100) NOT NULL,
    [QUERY_DATE] SMALLDATETIME DEFAULT (getdate()) NOT NULL,
    [START_DATE] SMALLDATETIME NOT NULL,
    [END_DATE]   SMALLDATETIME NOT NULL
);

