
CREATE VIEW dbo.V_AR_INVOICE_DATES( AR_INV_NBR, AR_TYPE, AR_POST_PERIOD, AR_INV_DATE, GLEXACT ) 
AS

	SELECT 
		AR_INV_NBR, 
		AR_TYPE, 
		MAX(AR_POST_PERIOD), 
		MAX(AR_INV_DATE),
		MAX(GLEXACT) 
	FROM dbo.ACCT_REC AS ar
	GROUP BY AR_INV_NBR, AR_TYPE

