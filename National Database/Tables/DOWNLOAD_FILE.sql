CREATE TABLE [dbo].[DOWNLOAD_FILE] (
    [DOWNLOAD_FILE_ID] INT           IDENTITY (1, 1) NOT NULL,
    [FILENAME]         VARCHAR (100) NOT NULL,
    [PROCESSED_TIME]   SMALLDATETIME NULL,
	[SKIPPED]		   BIT NOT NULL,
    CONSTRAINT [PK_PROCESSED_FILE] PRIMARY KEY CLUSTERED ([DOWNLOAD_FILE_ID] ASC) WITH (FILLFACTOR = 90)
);

