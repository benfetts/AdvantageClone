CREATE TABLE [dbo].[MARKET_BREAK] (
    [MARKET_BREAK_ID] INT          IDENTITY (1, 1) NOT NULL,
    [NTI_ID]          INT          NOT NULL,
    [SECTION]         VARCHAR (32) NOT NULL,
    [CATEGORY]        VARCHAR (50) NOT NULL,
    [IS_DELUXE]       BIT          NOT NULL,
    CONSTRAINT [PK_MARKET_BREAK] PRIMARY KEY CLUSTERED ([MARKET_BREAK_ID] ASC) WITH (FILLFACTOR = 90)
);

