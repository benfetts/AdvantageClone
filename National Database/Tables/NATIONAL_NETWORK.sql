CREATE TABLE [dbo].[NATIONAL_NETWORK] (
    [NATIONAL_NETWORK_ID] INT       IDENTITY (1, 1) NOT NULL,
    [CODE]                VARCHAR (6) NOT NULL,
    [NAME]                VARCHAR (32) NOT NULL,
    [VENUE_CODE]          VARCHAR (1)  NOT NULL,
    CONSTRAINT [PK_NATIONAL_NETWORK] PRIMARY KEY CLUSTERED ([NATIONAL_NETWORK_ID] ASC) WITH (FILLFACTOR = 90)
);

