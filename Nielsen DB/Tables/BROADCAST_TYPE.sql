CREATE TABLE [dbo].[BROADCAST_TYPE] (
    [BROADCAST_TYPE_ID] INT          NOT NULL,
    [NAME]              VARCHAR (20) NOT NULL,
    CONSTRAINT [PK_BROADCAST_TYPE] PRIMARY KEY CLUSTERED ([BROADCAST_TYPE_ID] ASC)
);

