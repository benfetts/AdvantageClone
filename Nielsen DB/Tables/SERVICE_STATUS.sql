CREATE TABLE [dbo].[SERVICE_STATUS] (
    [SERVICE_STATUS_ID] INT      NOT NULL,
    [LAST_RAN]          DATETIME NOT NULL,
    [IS_RUNNING]        BIT      NOT NULL,
    CONSTRAINT [PK_SERVICE_STATUS] PRIMARY KEY CLUSTERED ([SERVICE_STATUS_ID] ASC)
);

