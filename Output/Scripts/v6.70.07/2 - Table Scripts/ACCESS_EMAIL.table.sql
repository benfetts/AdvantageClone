IF EXISTS(SELECT TABLE_NAME FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_NAME = 'ACCESS_EMAIL') BEGIN

    DROP TABLE [dbo].[ACCESS_EMAIL]

END
GO

IF NOT EXISTS(SELECT TABLE_NAME FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_NAME = 'ACCESS_EMAIL') BEGIN

	CREATE TABLE [dbo].[ACCESS_EMAIL](
		[ACCESS_EMAIL_ID] [int] IDENTITY (1, 1) NOT NULL,
		[USER_CODE] [varchar](100) NOT NULL,
		[EMAIL_TO] [varchar](MAX) NOT NULL,
		[CC] [varchar](MAX) NOT NULL,
		[BCC] [varchar](MAX) NOT NULL,
		[SUBJECT] [varchar](MAX) NOT NULL,
		[IS_BODY_HTML] [bit] NOT NULL,
		[BODY] [varchar](MAX) NOT NULL,
		[ATTACHMENT_NAME] [varchar](MAX) NULL,
	    [ATTACHMENT] [varbinary](MAX) NULL,
		[ATTACHMENT_PATH] [varchar](MAX) NULL,
		[PROCESSED] [bit] NOT NULL,
		[ERROR_MESSAGE] [varchar](MAX) NOT NULL,
	 CONSTRAINT [PK_ACCESS_EMAIL_ID] PRIMARY KEY CLUSTERED 
	(
		[ACCESS_EMAIL_ID] ASC
	)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON) ON [PRIMARY]
    ) ON [PRIMARY]

END
GO
