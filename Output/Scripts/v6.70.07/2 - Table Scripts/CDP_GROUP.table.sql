IF NOT EXISTS(SELECT TABLE_NAME FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_NAME = 'CDP_GROUP') BEGIN

	CREATE TABLE [dbo].[CDP_GROUP](
		[CDP_GROUP_ID] [int] IDENTITY(1,1) NOT NULL,
		[NAME] [varchar](100) NOT NULL,
	CONSTRAINT [PK_CDP_GROUP] PRIMARY KEY CLUSTERED 
	(
		[CDP_GROUP_ID] ASC
	)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON, FILLFACTOR = 90) ON [PRIMARY]
	) ON [PRIMARY]

END
GO