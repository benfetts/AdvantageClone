IF NOT EXISTS(SELECT TABLE_NAME FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_NAME = 'CDP_GROUP_EMPLOYEE') BEGIN

	CREATE TABLE [dbo].[CDP_GROUP_EMPLOYEE](
		[CDP_GROUP_EMPLOYEE_ID] [int] IDENTITY(1,1) NOT NULL,
		[CDP_GROUP_ID] [int] NOT NULL,
		[EMP_CODE] [varchar](6) NOT NULL,
	CONSTRAINT [PK_CDP_GROUP_EMPLOYEE] PRIMARY KEY CLUSTERED 
	(
		[CDP_GROUP_EMPLOYEE_ID] ASC
	)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON, FILLFACTOR = 90) ON [PRIMARY]
	) ON [PRIMARY]
	
	ALTER TABLE [dbo].[CDP_GROUP_EMPLOYEE]  WITH CHECK ADD  CONSTRAINT [FK_CDP_GROUP_EMPLOYEE_CDP_GROUP] FOREIGN KEY([CDP_GROUP_ID])
	REFERENCES [dbo].[CDP_GROUP] ([CDP_GROUP_ID])
	
	ALTER TABLE [dbo].[CDP_GROUP_EMPLOYEE] CHECK CONSTRAINT [FK_CDP_GROUP_EMPLOYEE_CDP_GROUP]

	ALTER TABLE [dbo].[CDP_GROUP_EMPLOYEE]  WITH CHECK ADD  CONSTRAINT [FK_CDP_GROUP_EMPLOYEE_EMPLOYEE_CLOAK] FOREIGN KEY([EMP_CODE])
	REFERENCES [dbo].[EMPLOYEE_CLOAK] ([EMP_CODE])
	
	ALTER TABLE [dbo].[CDP_GROUP_EMPLOYEE] CHECK CONSTRAINT [FK_CDP_GROUP_EMPLOYEE_EMPLOYEE_CLOAK]

END
GO