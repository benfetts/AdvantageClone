-- DO NOT ALTER THIS AFTER 2/19
IF EXISTS(SELECT TABLE_NAME FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_NAME = 'EMPLOYEE_ADDL_EMAIL') BEGIN

    DROP TABLE [dbo].[EMPLOYEE_ADDL_EMAIL]

END
GO

IF NOT EXISTS(SELECT TABLE_NAME FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_NAME = 'EMPLOYEE_ADDL_EMAIL') BEGIN

	CREATE TABLE [dbo].[EMPLOYEE_ADDL_EMAIL](
		[EMPLOYEE_ADDL_EMAIL_ID] [int] IDENTITY(1,1) NOT NULL,
	    [EMP_CODE] [varchar](6) NOT NULL,
	    [EMP_EMAIL] [varchar](50) NULL,
     CONSTRAINT [PK_EMPLOYEE_ADDL_EMAIL] PRIMARY KEY CLUSTERED 
    (
	    [EMPLOYEE_ADDL_EMAIL_ID] ASC
    )WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON) ON [PRIMARY]
    ) ON [PRIMARY]
    
	ALTER TABLE [dbo].[EMPLOYEE_ADDL_EMAIL]  WITH CHECK ADD  CONSTRAINT [FK_EMPLOYEE_ADDL_EMAIL_EMPLOYEE_CLOAK] FOREIGN KEY([EMP_CODE])
	REFERENCES [dbo].[EMPLOYEE_CLOAK] ([EMP_CODE])

	ALTER TABLE [dbo].[EMPLOYEE_ADDL_EMAIL] CHECK CONSTRAINT [FK_EMPLOYEE_ADDL_EMAIL_EMPLOYEE_CLOAK]

END
GO