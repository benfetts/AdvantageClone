IF NOT EXISTS(SELECT TABLE_NAME FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_NAME = 'MARKET_GROUP') BEGIN

	CREATE TABLE [dbo].[MARKET_GROUP](
		[MARKET_GROUP_ID] [int] IDENTITY(1,1) NOT NULL,
		[MEDIA_TYPE] [varchar](1) NOT NULL,
		[COUNTRY_ID] [int] NOT NULL,
		[NAME] [varchar](100) NOT NULL,
	CONSTRAINT [PK_MARKET_GROUP] PRIMARY KEY CLUSTERED 
	(
		[MARKET_GROUP_ID] ASC
	)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON, FILLFACTOR = 90) ON [PRIMARY]
	) ON [PRIMARY]

	ALTER TABLE [dbo].[MARKET_GROUP]  WITH CHECK ADD  CONSTRAINT [FK_MARKET_GROUP_COUNTRY] FOREIGN KEY([COUNTRY_ID])
	REFERENCES [dbo].[COUNTRY] ([COUNTRY_ID])
	
	ALTER TABLE [dbo].[MARKET_GROUP] CHECK CONSTRAINT [FK_MARKET_GROUP_COUNTRY]

END
GO