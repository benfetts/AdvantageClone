IF NOT EXISTS(SELECT TABLE_NAME FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_NAME = 'MEDIA_BROADCAST_WORKSHEET_TIME_SEPARATION_SETTING') BEGIN

	CREATE TABLE [dbo].[MEDIA_BROADCAST_WORKSHEET_TIME_SEPARATION_SETTING](
		[MEDIA_BROADCAST_WORKSHEET_TIME_SEPARATION_SETTING_ID] [int] IDENTITY(1,1) NOT NULL,
		[MEDIA_BROADCAST_WORKSHEET_SPOT_MATCHING_SETTING_ID] [int] NOT NULL,
		[TIME_LENGTH] [int] NOT NULL,
		[TIME_SEPARATION] [int] NOT NULL,
		[CROSS_LENGTH_SEPARATION_ENABLED] [bit] NOT NULL,
		[CROSS_LENGTH_SEPARATION_VALUE] [smallint] NOT NULL,
	 CONSTRAINT [PK_MEDIA_BROADCAST_WORKSHEET_TIME_SEPARATION_SETTING] PRIMARY KEY CLUSTERED 
	(
		[MEDIA_BROADCAST_WORKSHEET_TIME_SEPARATION_SETTING_ID] ASC
	)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON, FILLFACTOR = 90) ON [PRIMARY]
	) ON [PRIMARY]
		
	ALTER TABLE [dbo].[MEDIA_BROADCAST_WORKSHEET_TIME_SEPARATION_SETTING]  WITH CHECK ADD  CONSTRAINT [FK_MEDIA_BROADCAST_WORKSHEET_TIME_SEPARATION_SETTING_MEDIA_BROADCAST_WORKSHEET_SPOT_MATCHING_SETTING] FOREIGN KEY([MEDIA_BROADCAST_WORKSHEET_SPOT_MATCHING_SETTING_ID])
	REFERENCES [dbo].[MEDIA_BROADCAST_WORKSHEET_SPOT_MATCHING_SETTING] ([MEDIA_BROADCAST_WORKSHEET_SPOT_MATCHING_SETTING_ID])

	ALTER TABLE [dbo].[MEDIA_BROADCAST_WORKSHEET_TIME_SEPARATION_SETTING] CHECK CONSTRAINT [FK_MEDIA_BROADCAST_WORKSHEET_TIME_SEPARATION_SETTING_MEDIA_BROADCAST_WORKSHEET_SPOT_MATCHING_SETTING]
	
END
GO
