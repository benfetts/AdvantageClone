IF NOT EXISTS(SELECT TABLE_NAME FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_NAME = 'MEDIA_BROADCAST_WORKSHEET_MARKET_SUBMARKET') BEGIN

	CREATE TABLE [dbo].[MEDIA_BROADCAST_WORKSHEET_MARKET_SUBMARKET](
		[MEDIA_BROADCAST_WORKSHEET_MARKET_SUBMARKET_ID] [int] IDENTITY(1,1) NOT NULL,
		[MEDIA_BROADCAST_WORKSHEET_MARKET_ID] [int] NOT NULL,
		[MARKET_CODE] [varchar](10) NOT NULL,
		[ORDER] [int] NOT NULL,
	 CONSTRAINT [PK_MEDIA_BROADCAST_WORKSHEET_MARKET_SUBMARKET] PRIMARY KEY CLUSTERED 
	(
		[MEDIA_BROADCAST_WORKSHEET_MARKET_SUBMARKET_ID] ASC
	)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON, FILLFACTOR = 90) ON [PRIMARY]
	) ON [PRIMARY]
	
	ALTER TABLE [dbo].[MEDIA_BROADCAST_WORKSHEET_MARKET_SUBMARKET]  WITH CHECK ADD  CONSTRAINT [FK_MEDIA_BROADCAST_WORKSHEET_MARKET_SUBMARKET_MEDIA_BROADCAST_WORKSHEET_MARKET] FOREIGN KEY([MEDIA_BROADCAST_WORKSHEET_MARKET_ID])
	REFERENCES [dbo].[MEDIA_BROADCAST_WORKSHEET_MARKET] ([MEDIA_BROADCAST_WORKSHEET_MARKET_ID])

	ALTER TABLE [dbo].[MEDIA_BROADCAST_WORKSHEET_MARKET_SUBMARKET] CHECK CONSTRAINT [FK_MEDIA_BROADCAST_WORKSHEET_MARKET_SUBMARKET_MEDIA_BROADCAST_WORKSHEET_MARKET]
	
	ALTER TABLE [dbo].[MEDIA_BROADCAST_WORKSHEET_MARKET_SUBMARKET]  WITH CHECK ADD  CONSTRAINT [FK_MEDIA_BROADCAST_WORKSHEET_MARKET_SUBMARKET_MARKET] FOREIGN KEY([MARKET_CODE])
	REFERENCES [dbo].[MARKET] ([MARKET_CODE])
	
	ALTER TABLE [dbo].[MEDIA_BROADCAST_WORKSHEET_MARKET_SUBMARKET] CHECK CONSTRAINT [FK_MEDIA_BROADCAST_WORKSHEET_MARKET_SUBMARKET_MARKET]

END
GO
