IF NOT EXISTS(SELECT COLUMN_NAME FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME = 'MEDIA_BROADCAST_WORKSHEET' AND COLUMN_NAME = 'COUNTRY_ID') BEGIN

	ALTER TABLE [dbo].[MEDIA_BROADCAST_WORKSHEET] ADD [COUNTRY_ID] [int] NOT NULL DEFAULT(1)
		
END
GO

