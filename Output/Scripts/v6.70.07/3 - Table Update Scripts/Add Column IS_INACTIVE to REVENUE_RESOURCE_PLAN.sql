--
IF NOT EXISTS (SELECT COLUMN_NAME FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME = 'REVENUE_RESOURCE_PLAN' AND COLUMN_NAME = 'IS_INACTIVE') BEGIN

	   ALTER TABLE [dbo].[REVENUE_RESOURCE_PLAN] ADD [IS_INACTIVE] [bit] NOT NULL DEFAULT(0)
	
END
GO