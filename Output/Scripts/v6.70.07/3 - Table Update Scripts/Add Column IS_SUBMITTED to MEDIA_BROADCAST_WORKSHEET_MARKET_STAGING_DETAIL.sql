IF NOT EXISTS (SELECT COLUMN_NAME FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME = 'MEDIA_BROADCAST_WORKSHEET_MARKET_STAGING_DETAIL' AND COLUMN_NAME = 'IS_SUBMITTED') BEGIN

	   ALTER TABLE [dbo].[MEDIA_BROADCAST_WORKSHEET_MARKET_STAGING_DETAIL] ADD [IS_SUBMITTED] [bit] NOT NULL DEFAULT(0)
	
END
GO
