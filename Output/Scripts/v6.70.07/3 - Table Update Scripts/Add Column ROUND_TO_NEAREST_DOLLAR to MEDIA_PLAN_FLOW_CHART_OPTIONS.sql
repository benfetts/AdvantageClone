IF NOT EXISTS (SELECT COLUMN_NAME FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME = 'MEDIA_PLAN_FLOW_CHART_OPTIONS' AND COLUMN_NAME = 'ROUND_TO_NEAREST_DOLLAR') BEGIN

	ALTER TABLE [dbo].[MEDIA_PLAN_FLOW_CHART_OPTIONS] ADD [ROUND_TO_NEAREST_DOLLAR] [bit] NOT NULL DEFAULT(0)
	
END
GO