IF NOT EXISTS(SELECT * FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_SCHEMA = 'dbo' AND TABLE_NAME = 'PROOFING_EXTERNAL')
BEGIN
	CREATE TABLE [dbo].[PROOFING_EXTERNAL](
		[ID] [int] IDENTITY(1,1) NOT NULL,
		[EMAIL] [varchar](1000) NOT NULL,
		[NAME] [varchar](1000) NULL,
	 CONSTRAINT [PK_PROOFING_EXTERNAL] PRIMARY KEY CLUSTERED 
	(
		[ID] ASC
	)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON) ON [PRIMARY]
	) ON [PRIMARY]
END
