IF NOT EXISTS(SELECT TABLE_NAME FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_NAME = 'VENDOR_DCM_MAPPING') BEGIN

	CREATE TABLE [dbo].[VENDOR_DCM_MAPPING](
		[VENDOR_DCM_MAPPING_ID] [int] IDENTITY(1,1) NOT NULL,
		[VN_CODE] [varchar](6) NOT NULL,
		[DC_PROFILE_ID] [bigint] NOT NULL,
		[DC_SITE_ID] [bigint] NOT NULL,
	 CONSTRAINT [PK_VENDOR_DCM_MAPPING] PRIMARY KEY CLUSTERED 
	(
		[VENDOR_DCM_MAPPING_ID] ASC
	)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON) ON [PRIMARY]
	) ON [PRIMARY]

	CREATE UNIQUE NONCLUSTERED INDEX [VENDOR_DCM_MAPPING_UNIQUE] ON [dbo].[VENDOR_DCM_MAPPING]
	(
		[VN_CODE] ASC,
		[DC_PROFILE_ID] ASC
	)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, SORT_IN_TEMPDB = OFF, IGNORE_DUP_KEY = OFF, DROP_EXISTING = OFF, ONLINE = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON)

END
GO