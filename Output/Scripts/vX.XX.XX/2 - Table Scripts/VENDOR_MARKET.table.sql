IF NOT EXISTS(SELECT TABLE_NAME FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_NAME = 'VENDOR_MARKET') BEGIN

	CREATE TABLE [dbo].[VENDOR_MARKET](
		[VENDOR_MARKET_ID] [int] IDENTITY(1,1) NOT NULL,
		[VN_CODE] [varchar](6) NOT NULL,
		[MARKET_CODE] [varchar](10) NOT NULL,
		[ORDER] [int] NOT NULL,
	CONSTRAINT [PK_VENDOR_MARKET] PRIMARY KEY CLUSTERED 
	(
		[VENDOR_MARKET_ID] ASC
	)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON, FILLFACTOR = 90) ON [PRIMARY]
	) ON [PRIMARY]

	ALTER TABLE [dbo].[VENDOR_MARKET]  WITH CHECK ADD  CONSTRAINT [FK_VENDOR_MARKET_MARKET] FOREIGN KEY([MARKET_CODE])
	REFERENCES [dbo].[MARKET] ([MARKET_CODE])
	
	ALTER TABLE [dbo].[VENDOR_MARKET] CHECK CONSTRAINT [FK_VENDOR_MARKET_MARKET]

	ALTER TABLE [dbo].[VENDOR_MARKET]  WITH CHECK ADD  CONSTRAINT [FK_VENDOR_MARKET_VENDOR] FOREIGN KEY([VN_CODE])
	REFERENCES [dbo].[VENDOR] ([VN_CODE])
	
	ALTER TABLE [dbo].[VENDOR_MARKET] CHECK CONSTRAINT [FK_VENDOR_MARKET_VENDOR]

END
GO