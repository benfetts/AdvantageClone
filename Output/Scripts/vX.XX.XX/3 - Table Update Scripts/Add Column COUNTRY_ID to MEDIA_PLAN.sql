IF NOT EXISTS(SELECT COLUMN_NAME FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME = 'MEDIA_PLAN' AND COLUMN_NAME = 'COUNTRY_ID') BEGIN

	ALTER TABLE [dbo].[MEDIA_PLAN] ADD [COUNTRY_ID] [int] NOT NULL DEFAULT(1)
		
    ALTER TABLE [dbo].[MEDIA_PLAN]  WITH CHECK ADD  CONSTRAINT [FK_MEDIA_PLAN_COUNTRY] FOREIGN KEY([COUNTRY_ID])
    REFERENCES [dbo].[COUNTRY] ([COUNTRY_ID])

    ALTER TABLE [dbo].[MEDIA_PLAN] CHECK CONSTRAINT [FK_MEDIA_PLAN_COUNTRY]

END
GO

