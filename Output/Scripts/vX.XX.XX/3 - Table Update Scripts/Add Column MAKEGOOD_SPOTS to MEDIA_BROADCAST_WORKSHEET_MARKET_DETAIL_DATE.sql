IF NOT EXISTS (SELECT COLUMN_NAME FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME = 'MEDIA_BROADCAST_WORKSHEET_MARKET_DETAIL_DATE' AND COLUMN_NAME = 'MAKEGOOD_SPOTS') BEGIN

	   ALTER TABLE [dbo].[MEDIA_BROADCAST_WORKSHEET_MARKET_DETAIL_DATE] ADD [MAKEGOOD_SPOTS] [int] NOT NULL DEFAULT(0)
	
END
GO
