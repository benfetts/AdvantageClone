IF NOT EXISTS (SELECT COLUMN_NAME FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME = 'GLRJEHDR' AND COLUMN_NAME = 'STARTING_PPPERIOD') BEGIN

	ALTER TABLE [dbo].[GLRJEHDR] ADD [STARTING_PPPERIOD] [varchar](6) NULL
	
	ALTER TABLE [dbo].[GLRJEHDR]  WITH CHECK ADD  CONSTRAINT [FK_GLRJEHDR_POSTPERIOD] FOREIGN KEY([STARTING_PPPERIOD])
	REFERENCES [dbo].[POSTPERIOD] ([PPPERIOD])
	
	ALTER TABLE [dbo].[GLRJEHDR] CHECK CONSTRAINT [FK_GLRJEHDR_POSTPERIOD]

END
GO