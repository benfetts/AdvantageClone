IF NOT EXISTS(	SELECT 1
				FROM   
					INFORMATION_SCHEMA.COLUMNS
				WHERE  
					TABLE_NAME = 'ALERT_RCPT'
					AND COLUMN_NAME = 'PROOFING_STATUS_ID') 
BEGIN
	ALTER TABLE ALERT_RCPT
	ADD PROOFING_STATUS_ID INT NULL;
END
IF NOT EXISTS(	SELECT 1
				FROM   
					INFORMATION_SCHEMA.COLUMNS
				WHERE  
					TABLE_NAME = 'ALERT_RCPT_DISMISSED'
					AND COLUMN_NAME = 'PROOFING_STATUS_ID') 
BEGIN
	ALTER TABLE ALERT_RCPT_DISMISSED
	ADD PROOFING_STATUS_ID INT NULL;
END
IF NOT EXISTS(	SELECT 1
				FROM   
					INFORMATION_SCHEMA.COLUMNS
				WHERE  
					TABLE_NAME = 'CP_ALERT_RCPT'
					AND COLUMN_NAME = 'PROOFING_STATUS_ID') 
BEGIN
	ALTER TABLE CP_ALERT_RCPT
	ADD PROOFING_STATUS_ID INT NULL;
END
IF NOT EXISTS(	SELECT 1
				FROM   
					INFORMATION_SCHEMA.COLUMNS
				WHERE  
					TABLE_NAME = 'CP_ALERT_RCPT_DISMISSED'
					AND COLUMN_NAME = 'PROOFING_STATUS_ID') 
BEGIN
	ALTER TABLE CP_ALERT_RCPT_DISMISSED
	ADD PROOFING_STATUS_ID INT NULL;
END