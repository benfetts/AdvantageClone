IF NOT EXISTS(SELECT COLUMN_NAME FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME = 'TIME_SEPARATION_SETTING' AND COLUMN_NAME = 'CROSS_LENGTH_SEPARATION_ENABLED') BEGIN

	ALTER TABLE [dbo].[TIME_SEPARATION_SETTING] ADD [CROSS_LENGTH_SEPARATION_ENABLED] [bit] NOT NULL DEFAULT(0)
	
END
GO

IF NOT EXISTS(SELECT COLUMN_NAME FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME = 'TIME_SEPARATION_SETTING' AND COLUMN_NAME = 'CROSS_LENGTH_SEPARATION_VALUE') BEGIN

	ALTER TABLE [dbo].[TIME_SEPARATION_SETTING] ADD [CROSS_LENGTH_SEPARATION_VALUE] [smallint] NOT NULL DEFAULT(0)
	
END
GO
