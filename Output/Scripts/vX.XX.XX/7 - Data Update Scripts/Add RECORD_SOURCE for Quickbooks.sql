IF NOT EXISTS(SELECT * FROM dbo.RECORD_SOURCE WHERE [NAME] = 'QuickBooks' AND SYSTEM_SOURCE = 1) BEGIN

	INSERT INTO dbo.RECORD_SOURCE 
		([NAME], [DESCRIPTION], [SYSTEM_SOURCE])
	VALUES
		('QuickBooks', 'QuickBooks Mappings', 1)

END
