UPDATE dbo.MEDIA_BROADCAST_WORKSHEET 
SET COUNTRY_ID = 1
WHERE COUNTRY_ID IS NULL
GO

IF NOT EXISTS(SELECT CONSTRAINT_NAME FROM INFORMATION_SCHEMA.REFERENTIAL_CONSTRAINTS WHERE CONSTRAINT_NAME = 'FK_MEDIA_BROADCAST_WORKSHEET_COUNTRY') BEGIN

	ALTER TABLE [dbo].[MEDIA_BROADCAST_WORKSHEET]  WITH CHECK ADD  CONSTRAINT [FK_MEDIA_BROADCAST_WORKSHEET_COUNTRY] FOREIGN KEY([COUNTRY_ID])
	REFERENCES [dbo].[COUNTRY] ([COUNTRY_ID])
	
	ALTER TABLE [dbo].[MEDIA_BROADCAST_WORKSHEET] CHECK CONSTRAINT [FK_MEDIA_BROADCAST_WORKSHEET_COUNTRY]

END
GO